def almostIncreasingSequence(sequence):
    for i in range(0, len(sequence)):# 1~3
        sequence()
        if pow(sequence[len(i)], 2) < pow(sequence[i], 2):
            seq.append(sequence[i])

    print(seq)
